{
  "name": "@moca/core",
  "version": "0.1.0",
  "description": "Core types, branded IDs, and shared utilities for Moca",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "dev": "tsc --build --watch",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "devDependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1051.0",
    "vitest": "^4.1.8"
  }
}
